#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @a: random no
 * Return: 'c' or 'a'
 */
int _abs(int a)
{
	if (a < 0)
	{
		int c = -c;
		
		return (c);
	}
	else
	{
		return (a);
	}
}
