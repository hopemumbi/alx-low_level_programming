#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @a: random no
 * Return: '-a' if a < 0 or 'a' if a > 0
 */
int _abs(int a)
{
	if (a < 0)
	{
		return (-a);
	}
	else
	{
		return (a);
	}
}
