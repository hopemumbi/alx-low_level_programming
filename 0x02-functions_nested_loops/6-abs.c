#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @a: random no
 * return 'c' or 'a'
 */
int _abs(int a)
{
	if (a < 0)
	{
		int b = -1;
		int c;

		c = a * b;
		return (c);
	}
	else
	{
		return (a);
	}
}
