#include "main.h"
/**
 *add - adds two integers and returns the result.
 *@a: integer one
 *@b: integer two
 *Return: the result
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
